# GUI chess game built on C++

Built as a training project for me to learn C++.



## Dependencies

### OpenGL
```
apt-get install libglfw3-dev libglew-dev libglm-dev
```

note useful commands:
```
find /usr -name "libglfw*"
```